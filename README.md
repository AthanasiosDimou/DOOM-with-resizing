# DOOM with Reisizing based on screensize

This is the code from the tautorial of code spacer at youtube but changed so that it fits for your screen


## problems that need to be addressed

* cause pywin32 was used, it does not work for linux

*  no requirements file existing so that you can download directly the packages

*  rendering is a bit messed up cause of the resizing

## How to run it 

to tell teh truth this is an old code thus, do not remember the exact packages but some of the packages I am certain of are `pywin32`, `pygame`.

so download the files needed and then python `main.py`

## Logic

The logic for this game is not that it uses 3d. rather it uses linear algebra so that with transformation matrix it teraforms 2d into 3d perception

For that exact reason when making the changes for the resizing, in most probability, I have forgotten to take considaration of something or change some parameter as should. So to play, 
for the NPCs that do not respond you should go left of them at the height in the map they exist. In other words go at their left and then run before they kill you.
